srcBase=source
resBase=resources

# time and frequency domain based processing
base.sourcePath=$(srcBase);source-timefrequency
base.resourcePath=$(resBase)

#fenix5x.sourcePath = $(srcBase);source-maps
#fenix5x.resourcePath = $(resBase);resources-maps