const MAX_TIME = 6; // minutes. Need to change mMaxBuf if this changes
const MAX_BUF = 960; // maximum buffer size if HR = 160 with minutes above
const M_SECS = 300; // number of seconds. needs to tie to settings and property limits
const SMALL = true;