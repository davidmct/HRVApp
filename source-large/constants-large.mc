const MAX_TIME = 20; // minutes. Need to change mMaxBuf if this changes
const MAX_BUF = 3200; // maximum buffer size if HR = 160 with minutes above
const M_SECS = 1200; // number of seconds. needs to tie to settings and property limits
const SMALL = false;